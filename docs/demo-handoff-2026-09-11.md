# Doc Agent 手动验收交接文档（2026-09-11）

> 交接日期：2026-09-11
> 交接原因：人工业务验收进行到中段，转交其他 AI 继续
> 分支：`feature/doc-agent-foundation`（仅本地，未 push、未 merge）
> HEAD：`80ddbd7`（`docs: add demo handoff for workbench foundation`）
> 上轮交接：`docs/demo-handoff-2026-08-24.md`（上上轮；本轮文档只记录**增量与当前状态**）

## 〇、工作区状态（重要）

- **有 53 个文件未提交**：38 个已修改（M）+ 15 个新增（??），覆盖后端评审/治理/质量模块与前端评审工作区、治理、管理、项目模块。
- 上一轮（08-24 之后）已经提交了 5 个新提交，但最后一次大改动（评审工作区、ReviewQueue、ReviewWorkspace、ManagementWorkspace、知识治理等）**还停留在工作区里没提交**。
- **交接前不要 `git checkout` / `git reset` / `git stash` 清理工作区**，这些未提交改动就是当前要验收的功能本体。
- 截图/数据库/`storage/` 等本地工件已 gitignore，不要删除。

## 一、服务状态与重启方法

两个服务**当前已在运行**（后端由我本会话启动，前端 PID 39512 为上一会话残留但存活）：

| 服务 | 地址 | 状态 |
|---|---|---|
| 后端 + 独立 Worker | http://127.0.0.1:8000 | `/api/health` → `{"status":"ready"}` |
| 前端 Vite | http://127.0.0.1:5173 | 200 OK |
| API 文档 | http://127.0.0.1:8000/docs | 可用 |

重启命令（项目目录 `D:\workspace1\doc-agent-worktrees\foundation`）：

```bash
# 后端 + 独立 Worker（一条命令启动两者）
uv run --directory D:/workspace1/doc-agent-worktrees/foundation python run_dev.py

# 前端
npm --prefix D:/workspace1/doc-agent-worktrees/foundation/web run dev -- --host 127.0.0.1 --port 5173
```

- 登录账号：`demo / DemoPass-2026!`（仅本地演示）
- 启动逻辑说明：`run_dev.py` 会先跑 Alembic 迁移 → 启动 uvicorn(8000) → 再启动 `run_worker.py`（独立 Worker 进程）；支持 `--no-worker`、`--no-migrate`。
- **故障经验**：前端"启动任务被杀但 Vite 还活着"是常见现象（npm 把 Vite 留成了孤儿进程）；判断服务是否真在跑，看端口监听 `netstat -ano | grep :5173`，别只看启动任务。

## 二、已完成的验收项（全部通过）

| # | 测试项 | 结果 |
|---|---|---|
| 1 | 登录（demo / DemoPass-2026!） | 通过 |
| 2 | Agent 基础问答（输入"请检查当前文档"） | 正常返回，使用本地 FakeProvider |
| 3 | 文档正文加载（演示项目：年度审计报告 / 2026 年上半年审计整改报告 / v1） | 正常显示 |
| 4 | 知识库检索（关键词"采购比价"） | 返回相关结果，来源定位正确 |
| 5 | v1 → v2 上传（`C:\Users\11780\Downloads\审计整改报告-v2-测试.md`） | v2 创建成功，版本列表显示 v1、v2 |
| 6 | 语义对比 | 任务完成，**但页面只显示字数差异**（v1: 158 字 / v2: 318 字），未见具体新增/删除/修改内容 |
| 7 | 写作契约模拟评审（v2） | 成功，显示 7/7 已满足，**但所有条目错误显示为"建议"**，原本"必需满足"项也被显示为建议 |
| 8 | 创建评审 | 成功 |

## 三、当前中断点（接手后从这里继续）

**文档：采购比价整改方案** — 评审页当前实际状态：

```
评审
审核中
绑定版本：v1 · 工作流修订号：3 · 你是本评审的评审员
请求修改 / 批准
```

**⚠️ 关键业务一致性问题**：文档已有 v1、v2 两个版本，但本评审**绑定的是 v1**（此前预期验证的是绑定 v2）。**先不要点击"批准"**，保留此现场供排查"创建评审时版本绑定逻辑"。

排查线索方向（未验证）：
- 前端创建评审时是否传了当前选中版本（`web/src/features/reviews/ReviewPanel.tsx` 及相关新建入口）
- 后端 `app/reviews/service.py` / `schemas.py` 创建时的默认绑定逻辑

## 四、未完成的验收清单（按顺序继续）

1. **评审员"请求修改"**：意见示例 `请补充三家供应商报价材料的归档位置，并明确历史采购事项的责任人和完成标准。` → 确认状态变"需修改"
2. **作者返修**：切换作者身份 → 查看修改意见 → 修改正文或上传新版本
3. **重新提交**：确认状态回到"审核中"，**核对绑定版本是否更新为新版本**
4. **评审员批准** → 状态"已批准"
5. **晋升质量门** → 文档进入可索引状态
6. **Worker 索引** → 观察任务处理与状态流转
7. **知识库检索新内容**：用返修内容的独特关键词搜索 → 应命中新版本
8. **撤销后检索结果消失**：撤销质量门/发布 → 检索该内容应消失
9. **权限隔离与越权访问**：无权限账号访问文档/评审/API → 应被拒绝
10. **前端问题统一复测**（见下节问题一、二）

## 五、已发现的前端问题（待修）

### 问题一：知识库详情缺少返回入口

知识库搜索结果点入文档片段后：来源定位正确，但**没有"返回知识库"按钮**，只能靠左侧导航或浏览器后退。

### 问题二：子页面返回正文后版本状态丢失

从"评审"或"写作契约"切回"正文"后：左侧文档还在，但**版本按钮和正文内容消失**；手动刷新后 v1、v2 恢复 → 说明后端数据未丢，是**前端页面内导航后版本状态未重新加载**。

初步代码定位：

| 文件 | 职责 |
|---|---|
| `web/src/app/App.tsx:90-151` | 文档、版本、正文的异步加载和状态恢复 |
| `web/src/features/documents/DocumentHub.tsx:16-30` | 正文/评审/写作契约/版本对比标签 |
| `web/src/features/contracts/WritingContractForm.tsx` | 写作契约页面 |
| `web/src/features/comparison/ComparisonView.tsx` | 版本对比展示 |

### 问题三：语义对比只显示字数差异

页面无新增/删除/修改内容（红绿 diff 可视化未实现，属 08-24 交接已知缺陷延续）。

### 问题四：契约模拟评审"必需满足"显示为"建议"

优先级：问题二的排查与第四节第 1-4 项验收并行，因为评审流转验证依赖页面状态稳定性。

## 六、上线判断（勿夸大）

**当前不能宣称生产就绪**，原因：

- 真实 Provider 运行时装配链未确认（演示用 FakeProvider）
- 知识库依赖 SQLite
- 备份恢复、限流、监控告警、TLS、多实例能力均未验证
- 前端存在返回导航、版本状态、差异展示三类问题

**当前适合**：本机演示；受控内网、单实例、少量用户、合成/低敏数据的私有 Beta。

**当前不适合**：公网生产；真实敏感审计资料；多实例高并发；直接接入真实模型流量。

## 七、注意事项

- **不要 push / merge**：分支仅本地；`doc_agent.db`、`storage/`、`web/.edge-profile/`、截图均为本地工件已 gitignore
- **不要清空工作区**：53 个未提交文件是验收功能本体（见第〇节）
- **评审"批准"按钮暂时不要点**：v1/v2 绑定问题未查清前保留现场
- 数据库在 **SQLite**（`doc_agent.db`，项目根）→ 停服务不丢数据，重启即恢复
- 规格文档：`docs/superpowers/specs/2026-08-18-doc-agent-design.md`；实施计划：`docs/superpowers/plans/2026-08-18-doc-agent-foundation.md`
- 上轮交接（历史背景与完整功能基线）：`docs/demo-handoff-2026-08-24.md`

## 八、快速自检命令

```bash
git -C D:/workspace1/doc-agent-worktrees/foundation status --short   # 应显示 ~53 个未提交文件
curl http://127.0.0.1:8000/api/health                                 # {"status":"ready"}
curl -I http://127.0.0.1:5173/                                        # 200 OK
```
